# Leave empty in development so requests use `/api` and Vite proxies to the server (required for login cookies).
# In production, set to your public API origin if it differs from the site, e.g. https://api.example.com
VITE_API_URL=
